#include <stdio.h>

int main() {
    int soma = 0, numero;

    for (numero = 3; numero <= 500; numero += 3) {
    
        if (numero % 2 != 0 && numero % 3 == 0) {
            soma += numero;
        }
    }
    printf("\n - Essa e a soma: %i", soma);
  
    return 0;
}
